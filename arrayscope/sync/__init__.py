"""Linked-window synchronization (inspection groups) for ArrayScope.

``messages`` is Qt-free message vocabulary and codec; ``bus`` is the
cross-process transport (Qt local sockets); ``controller`` binds one
window to the bus with per-facet participation toggles.
"""
