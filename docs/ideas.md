# Ideas

## Candidate features
- ...

## Technical debt
- Preserve axis identity metadata across shape-changing operation stacks so UI
  labels and domain state can track source dimensions after reductions.
- Add first-class scalar display support for operation stacks that reduce all
  dimensions.
- Implement bounded nearby-slice prefetching behind the existing setting, with
  cache keys invalidated by operation stack, view axes, channel/scale mode,
  windowing mode, and profile role changes.
- Add targeted pyqtgraph readability fixes for optional theme backends if a
  backend makes histogram or plot foreground/background colors hard to read.

## UX ideas
- ...

## Maybe never
- ...
