# ArrayShow reference notes

Local reference checkout:
`docs/references/ArrShow` at commit `<hash>`.

Useful ideas to port:
- dimension right-click operations
- plot dimension selection
- crop/reduction/FFT operations
- synchronized windows

Do not copy blindly:
- destructive default operations
- MATLAB workspace assumptions
- UI layout choices